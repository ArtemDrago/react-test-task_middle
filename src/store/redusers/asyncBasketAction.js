import { createAsyncThunk } from "@reduxjs/toolkit";

// export const addBasketItem = createAsyncThunk(
//    "catalog/addBasketItem",
//    async (key, item) => {

//      return [key, item];
//    }
// );